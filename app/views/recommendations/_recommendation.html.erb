<div class="rounded-lg border p-4 shadow-md">
  <img src="<%= recommendation.book.image_url %>" alt="<%= recommendation.book.title %>" class="w-full h-48 object-cover rounded">
  <h3 class="text-xl font-bold"><%= recommendation.book.title %></h3>
  <p class="text-sm text-gray-500">by <%= recommendation.book.author %></p>
  <p class="mt-2 text-gray-700">"<%= truncate(recommendation.review, length: 100) %>"</p>
  <%= button_to "▲ Vote (#{recommendation.votes.count})", recommendation_votes_path(recommendation), method: :post, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-3 rounded" %>
</div>